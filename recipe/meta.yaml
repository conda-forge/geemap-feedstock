{% set name = "geemap" %}
{% set version = "0.16.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 071df49472fa530ef5d6962ff2e0c5a957cf628ece93942bf0c80c2ac599e18c

build:
  number: 0
  entry_points:
    - geemap=geemap.cli:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.7
  run:
    - bqplot
    - colour
    - earthengine-api >=0.1.316
    - ee_extra >=0.0.10
    - ffmpeg-python
    - folium >=0.11.0
    - gdown
    - geeadd >=0.5.1
    - geocoder
    - geojson
    - ipyevents
    - ipyfilechooser >=0.6.0
    - ipyleaflet >=0.14.0
    - ipytree
    - jupyterlab >=3.0.0
    - matplotlib-base
    - numpy
    - pandas
    - pillow
    - pycrs
    - pyshp >=2.1.3
    - python >=3.7
    - python-box
    - sankee
    - scooby
    - whiteboxgui >=0.6.0
    - xyzservices

test:
  imports:
    - geemap
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://geemap.org
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: Earth Engine Python API and interactive mapping
  description: A Python package for interactive mapping with Google Earth Engine, ipyleaflet, and folium.
  doc_url: https://geemap.org
  dev_url: https://github.com/giswqs/geemap

extra:
  recipe-maintainers:
    - giswqs
